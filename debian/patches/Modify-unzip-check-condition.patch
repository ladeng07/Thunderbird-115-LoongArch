--- thunderbird-68.7.0.orig/toolkit/moz.configure
+++ thunderbird-68.7.0/toolkit/moz.configure
@@ -675,7 +675,6 @@ set_config('MOZ_GPSD', depends_if(system
 # ==============================================================

 check_prog('TAR', ('gnutar', 'gtar', 'tar'))
-check_prog('UNZIP', ('unzip',))
 check_prog('ZIP', ('zip',))
 check_prog('GN', ('gn',), allow_missing=True)
